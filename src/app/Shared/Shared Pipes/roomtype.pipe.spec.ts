import { RoomtypePipe } from './roomtype.pipe';

describe('RoomtypePipe', () => {
  it('create an instance', () => {
    const pipe = new RoomtypePipe();
    expect(pipe).toBeTruthy();
  });
});
